# Submission Checklist — Tax Calculator Final Project

Fill in the `[ YOUR ACTUAL OUTPUT HERE ]` placeholders with your own real
terminal output and screenshots. Do not submit fabricated results.

---

## TASK 1 — Run Unit Tests Using Jasmine

**Command:**
```bash
npx jasmine
```

**Expected evidence:**
```
7 specs, 0 failures
```

**Save terminal output as:** `01-jasmine-tests-passing`

**Your actual output:**
```
[ YOUR ACTUAL OUTPUT HERE ]
```

**Files to submit:** `src/taxCalculator.js`, `spec/taxCalculatorSpec.js`, `spec/support/jasmine.json`

---

## TASK 2 — Create the Dockerfile

**File to submit:** `Dockerfile` (already created — see project root)

**Save/capture as:** `02-Dockerfile`

---

## TASK 3 — Build the Docker Image

**Command:**
```bash
docker build -t tax-calculator .
```

**Expected evidence:** A build log ending in `Successfully tagged tax-calculator:latest` (or equivalent BuildKit success output).

**Save terminal output as:** `03-docker-build-success`

**Your actual output:**
```
[ YOUR ACTUAL OUTPUT HERE ]
```

---

## TASK 4 — Deploy and Test the Web Application in a Docker Container

**Commands:**
```bash
docker run -p 8080:8080 tax-calculator
```

Then open in a browser:
```
http://localhost:8080
```

Also verify the health endpoint:
```bash
curl http://localhost:8080/health
```

**Save evidence as:**
- `04-docker-run-terminal`
- `04-tax-calculator-browser-screenshot`

**Your actual output:**
```
[ YOUR ACTUAL OUTPUT HERE ]
```

---

## TASK 5 — Tag and Push to IBM Cloud Registry

**Commands:**
```bash
ibmcloud login --sso
ibmcloud target -r <YOUR_IBM_REGION>
ibmcloud cr login
docker tag tax-calculator <YOUR_IBM_REGION>.icr.io/<YOUR_REGISTRY_NAMESPACE>/tax-calculator:1.0
docker push <YOUR_IBM_REGION>.icr.io/<YOUR_REGISTRY_NAMESPACE>/tax-calculator:1.0
```

**Save terminal output as:** `05-registry-tag-push`

**Your actual output:**
```
[ YOUR ACTUAL OUTPUT HERE ]
```

---

## TASK 6 — Deploy the Tax Calculator on IBM Cloud

**Commands:**
```bash
ibmcloud ce project select --name <YOUR_CE_PROJECT_NAME>
ibmcloud ce application create \
  --name tax-calculator \
  --image <YOUR_IBM_REGION>.icr.io/<YOUR_REGISTRY_NAMESPACE>/tax-calculator:1.0 \
  --port 8080 \
  --registry-secret <YOUR_REGISTRY_SECRET_NAME>
ibmcloud ce application get --name tax-calculator -o url
```

**Evidence to capture:**
- Terminal output of the deployment
- The real, generated Code Engine URL (do not fabricate this)
- A screenshot of the deployed application in the browser

**Save as:** `06-codeengine-deploy` and `06-deployed-app-screenshot`

**Your actual output / URL:**
```
[ YOUR ACTUAL OUTPUT / URL HERE ]
```

---

## TASK 7 — Create the Tekton Pipeline Tasks

**File to submit:** `tekton/tasks.yaml` (already created — contains 4 Tasks: `install-dependencies`, `run-tests`, `build-image`, `deploy-image`)

**Save/capture as:** `07-tasks-yaml`

---

## TASK 8 — Extend the Pipeline to Call Required Tasks

**File to submit:** `tekton/pipeline.yaml` (already created — chains the 4 tasks in order using `runAfter`)

**Save/capture as:** `08-pipeline-yaml`

---

## TASK 9 — Run the Tekton Pipeline

**Commands:**
```bash
kubectl apply -f tekton/tasks.yaml
kubectl apply -f tekton/pipeline.yaml

tkn pipeline start tax-calculator-pipeline \
  --workspace name=shared-workspace,claimName=<YOUR_PVC_NAME> \
  --param IMAGE=<YOUR_IBM_REGION>.icr.io/<YOUR_REGISTRY_NAMESPACE>/tax-calculator:pipeline \
  --param CE_PROJECT=<YOUR_CE_PROJECT_NAME> \
  --param CE_APP_NAME=tax-calculator \
  --showlog

tkn pipelinerun logs --last -f
```

**Evidence to capture:**
- Terminal output showing the `install`, `test`, `build`, and `deploy` tasks each running
- Confirmation that the `test` task shows the Jasmine `7 specs, 0 failures` output
- Confirmation the image was built (from the `build` task logs)

**Save as:** `09-pipelinerun-logs`

**Your actual output:**
```
[ YOUR ACTUAL OUTPUT HERE ]
```

---

## TASK 10 — Deploy the Image Built Using the Pipeline

**Evidence to capture:**
- The `deploy` task's log output showing `ibmcloud ce application update/create` succeeding
- The resulting Code Engine URL
- A screenshot of the application running, confirming it was the pipeline-built image

**Save as:** `10-pipeline-deploy-output` and `10-final-app-screenshot`

**Your actual output / URL:**
```
[ YOUR ACTUAL OUTPUT / URL HERE ]
```

---

## Final Notes

- Do not submit fabricated command output, image IDs, or URLs.
- Every placeholder above (`<YOUR_...>` and `[ YOUR ACTUAL OUTPUT HERE ]`)
  must be replaced with something you personally generated by running the
  commands in your own environment.
- If any step fails, capture the actual failure output — that's still valid
  evidence of the process, and you can note the fix you applied.
